/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { InventoryLedgerService } from "../inventoryLedger.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { InventoryLedgerCreateInput } from "./InventoryLedgerCreateInput";
import { InventoryLedgerWhereInput } from "./InventoryLedgerWhereInput";
import { InventoryLedgerWhereUniqueInput } from "./InventoryLedgerWhereUniqueInput";
import { InventoryLedgerFindManyArgs } from "./InventoryLedgerFindManyArgs";
import { InventoryLedgerUpdateInput } from "./InventoryLedgerUpdateInput";
import { InventoryLedger } from "./InventoryLedger";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class InventoryLedgerControllerBase {
  constructor(
    protected readonly service: InventoryLedgerService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "InventoryLedger",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: InventoryLedger })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: InventoryLedgerCreateInput
  ): Promise<InventoryLedger> {
    return await this.service.create({
      data: {
        ...data,

        inventory: {
          connect: data.inventory,
        },
      },
      select: {
        amount: true,
        createdAt: true,
        id: true,

        inventory: {
          select: {
            id: true,
          },
        },

        productId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "InventoryLedger",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [InventoryLedger] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(InventoryLedgerFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<InventoryLedger[]> {
    const args = plainToClass(InventoryLedgerFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        amount: true,
        createdAt: true,
        id: true,

        inventory: {
          select: {
            id: true,
          },
        },

        productId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "InventoryLedger",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: InventoryLedger })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: InventoryLedgerWhereUniqueInput
  ): Promise<InventoryLedger | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        amount: true,
        createdAt: true,
        id: true,

        inventory: {
          select: {
            id: true,
          },
        },

        productId: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "InventoryLedger",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: InventoryLedger })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: InventoryLedgerWhereUniqueInput,
    @common.Body() data: InventoryLedgerUpdateInput
  ): Promise<InventoryLedger | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          inventory: {
            connect: data.inventory,
          },
        },
        select: {
          amount: true,
          createdAt: true,
          id: true,

          inventory: {
            select: {
              id: true,
            },
          },

          productId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "InventoryLedger",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: InventoryLedger })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: InventoryLedgerWhereUniqueInput
  ): Promise<InventoryLedger | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          amount: true,
          createdAt: true,
          id: true,

          inventory: {
            select: {
              id: true,
            },
          },

          productId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
